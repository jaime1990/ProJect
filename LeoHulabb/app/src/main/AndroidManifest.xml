<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leohulabb" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/TranslucentTheme" >
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >

            <!--使用全屏style，同时代码中隐藏状态栏便可实现状态栏隐藏效果-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".splash.MainActivity"/>

        <activity
            android:name=".testmsp.TestListActivityActivity"/>

        <activity
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:name=".splash.GuideActivity"/>

        <activity
            android:name=".testmsp.TestActivityActivity"/>

        <activity
            android:name=".module.login.TestReboundActivity"/>
    </application>

</manifest>
